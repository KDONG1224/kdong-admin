import { Modal } from 'antd';
import styled from 'styled-components';

export const StyledCategoryEditModal = styled(Modal)`
  .edit-wrapper {
    &-title {
    }

    &-body {
      padding: 1rem 0;

      &-desc {
        margin-bottom: 1.2rem;
      }
    }
  }
`;
