// layouts
import { MainLayout } from 'layouts';

// containers
import { ProfileBanner } from 'containers';

const ProfileBannerPage = () => {
  return (
    <MainLayout>
      <ProfileBanner />
    </MainLayout>
  );
};

export default ProfileBannerPage;
